<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solomarket.dao.CommentDao">
    <insert id="insertComment" parameterType="com.solomarket.dto.CommentDto">
        INSERT INTO tbl_comment (board_id, user_no, parent_id, content, created_at)
        VALUES (#{boardId}, #{userNo}, #{parentId}, #{content}, NOW())
    </insert>

    <!-- 특정 게시글의 댓글 목록 -->
    <select id="selectCommentsByBoardId" resultType="com.solomarket.dto.CommentDto">
        SELECT c.*, u.nickname
        FROM tbl_comment c
        JOIN tbl_user u ON c.user_no = u.user_no
        WHERE c.board_id = #{boardId}
        ORDER BY c.created_at ASC
    </select>

    <!-- 댓글 1개 조회 -->
    <select id="selectCommentById" resultType="com.solomarket.dto.CommentDto">
        SELECT * FROM tbl_comment WHERE comment_id = #{commentId}
    </select>

    <delete id="deleteComment">
        DELETE FROM tbl_comment WHERE comment_id = #{commentId}
    </delete>

    <select id="getCommentsByBoardId" resultType="com.solomarket.dto.CommentDto">
        SELECT
        c.comment_id,
        c.board_id,
        c.user_no,
        c.parent_id,
        c.content,
        c.created_at,
        u.nickname
        FROM tbl_comment c
        LEFT JOIN tbl_user u ON c.user_no = u.user_no
        WHERE c.board_id = #{boardId}
        ORDER BY c.created_at ASC
    </select>

    <insert id="addComment" parameterType="com.solomarket.dto.CommentDto">
        INSERT INTO tbl_comment (board_id, user_no, parent_id, content, created_at)
        VALUES (#{boardId}, #{userNo}, #{parentId}, #{content}, NOW())
    </insert>
</mapper>
